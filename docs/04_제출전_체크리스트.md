# 04. 제출 전 체크리스트 (과제 요구사항 대조표)

> 과제 PDF의 "4. 기능 요구 사항" 순서 그대로입니다.
> 평가 전에 **하나씩 직접 눌러 보며** 체크하세요. 학칙상 **동작하지 않는 코드에 점수를 주면
> 평가자·피평가자 양측 모두 경고** 대상입니다.

## 1. 프로젝트 기본 구성
- [x] `index.html` / `css/` / `js/` / `images/` 역할 분리
- [x] 외부 스타일시트·스크립트를 HTML에 올바르게 연결
- [ ] **VS Code + Live Server 로 실행해 확인** ← 직접 하세요

## 2. HTML 구조 (시맨틱 마크업)
- [x] `header` `nav` `main` `section` `footer` 사용 (index.html)
- [x] `article` 사용 — 프로젝트 카드가 `article` 입니다. **JS가 생성**하므로 index.html 소스에는 없고,
      브라우저 개발자도구 Elements 탭에서 확인됩니다. (동료가 물으면 이렇게 답하세요)
- [x] Hero / About / Skills / Projects / Contact / Footer 6개 영역 존재
- [x] 네비게이션 앵커 링크(`#hero` … `#contact`)
- [x] 모든 이미지에 의미 있는 `alt` (장식 SVG는 `aria-hidden`)
- [x] `label[for]` ↔ `input[id]` 매칭 (name / email / message)

## 3. CSS (레이아웃 & 반응형)
- [x] 외부 스타일시트 `css/style.css`
- [x] `:root` CSS 변수로 색·폰트·간격 정의
- [x] `[data-theme="dark"]` 다크 모드 변수 별도 정의
- [x] 네비게이션 **Flexbox** (로고 왼쪽 / 메뉴 오른쪽)
- [x] Projects 카드 **Grid** (`auto-fit`, `minmax`)
- [x] 모바일 퍼스트 + 브레이크포인트 768px / 1024px
- [x] 모바일에서 메뉴 숨김 + 햄버거 노출
- [x] 버튼·카드 hover + `transition`, 카드 `box-shadow`

## 4. JavaScript 기초
- [x] `defer` 속성으로 연결
- [x] `var` 없이 `const` / `let` 만 사용
- [x] HTML `onclick` 없이 `addEventListener` 만 사용
- [x] `querySelector` / `querySelectorAll`
- [x] `textContent` / `innerHTML`
- [x] `classList.add` / `remove` / `toggle`
- [x] `click` / `submit` / `scroll` / `input` 이벤트 모두 사용
- [x] `event.preventDefault()` (링크 이동, 폼 제출)

## 5. 인터랙션
- [x] a. 햄버거 메뉴 토글 (`classList.toggle`)
- [x] b. 부드러운 스크롤
- [x] c. 스크롤 탑 버튼 — **300px** (README 명시)
- [x] d. 네비 배경 변경 — **60px** (README 명시)
- [x] e. 다크 모드 + localStorage 유지
- [x] f. Intersection Observer 등장 애니메이션 — **threshold 0.2** (README 명시)

## 6. 폼 UX
- [x] 이름 / 이메일 / 메시지 필드
- [x] 필수값 검증 (빈 필드 제출 불가)
- [x] 이메일 형식 검증 (정규식)
- [x] 에러 메시지가 해당 입력 필드 바로 아래 표시
- [x] `preventDefault()` + 성공 메시지

## 7. ES6+ 문법 & 배열 메서드
- [x] 화살표 함수
- [x] 템플릿 리터럴로 HTML 동적 생성
- [x] 구조분해 할당 (`createProjectCard`, 옵저버 콜백, `PROFILE`)
- [x] `map` (카드 생성) / `filter` (포크 제외·언어 필터) / `forEach` (이벤트 등록)

## 8. 비동기 & API
- [x] `fetch` + `async/await`
- [x] 엔드포인트 `https://api.github.com/users/{아이디}/repos`
- [x] 로딩 상태 (스피너 + 스켈레톤)
- [x] 성공 상태 (카드 리스트)
- [x] 에러 상태 (메시지 + **재시도 버튼**)
- [x] 빈 상태 (표시할 프로젝트가 없습니다)
- [x] `try/catch` + `response.ok` 확인 + 403 레이트리밋 분기

## 9. 상태 관리 패턴
- [x] `state` 객체 + 렌더 함수 분리
- [x] 흐름 ① 다크모드 토글 → 테마 상태 → 전체 스타일
- [x] 흐름 ② API 호출 → 로딩/성공/에러 → Projects 렌더링
- [x] 흐름 ③ 폼 입력 → 유효성 상태 → 에러 메시지
- [x] 흐름 ④ 필터 클릭 → 필터 상태 → 목록 변경 (선택 과제)

## 10. 배포 — ⛳ 여기부터는 직접 해야 합니다
- [ ] `js/config.js` 의 `githubUsername` 을 **본인 아이디로 변경**
- [ ] `index.html` 의 이름·소개·이메일을 본인 것으로 변경
- [ ] GitHub 저장소 생성 후 push
- [ ] Settings → Pages → main / (root) 배포
- [ ] **배포 URL에서 모든 기능 재확인** (로컬에서만 되는 경우 많음)
- [ ] 스크린샷 3장(데스크톱/모바일/다크모드) 촬영 → `images/` 저장
- [ ] `README.md` 상단의 배포 URL·저장소 URL 채우기

## 보너스 과제 (선택)
- [x] 1. 언어별 프로젝트 필터링 (`filter`)
- [x] 2. Hero 타이핑 효과
- [ ] 3. Formspree / EmailJS 실제 전송 — 미구현 (원하면 추가)
- [x] 4. `prefers-color-scheme` 시스템 다크 모드 감지

## 제약 사항 준수
- [x] React / Vue / jQuery / Bootstrap / Tailwind **미사용**
- [x] 인라인 스타일 `style="..."` **없음**
- [x] HTML `onclick` **없음**
- [x] `var` **없음**
- [x] 최신 Chrome 정상 동작 확인

---

## 🧪 배포 후 최종 테스트 (5분)

| 테스트 | 방법 | 기대 결과 |
| --- | --- | --- |
| 반응형 | 개발자도구 → 기기 툴바 → iPhone / iPad / 데스크톱 | 레이아웃 깨짐 없음, 가로 스크롤 없음 |
| 햄버거 | 모바일 폭에서 버튼 클릭 | 메뉴 열림 → X 모양으로 변형 → 항목 클릭 시 닫힘 |
| 다크모드 | 토글 후 **새로고침** | 다크 유지 |
| 프로젝트 | 새로고침 | 스피너 → 카드 |
| 에러 | 개발자도구 Network → Offline → 새로고침 | 에러 메시지 + 재시도 버튼 |
| 폼 | 빈 채로 제출 / 이메일에 `abc` | 필드 아래 에러 3개 / 형식 오류 메시지 |
| 폼 성공 | 정상 입력 후 제출 | 성공 메시지 + 입력창 비워짐 |
| 콘솔 | F12 → Console | **빨간 에러 0개** |
